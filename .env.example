# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.4

# Agent URLs — where the orchestrator connects to sub-agents
RECRUITING_AGENT_URL=http://localhost:5001
BROWSER_AGENT_URL=http://localhost:5002

# MCP URL — where the recruiting agent connects to the MCP server
MCP_URL=http://localhost:5003
