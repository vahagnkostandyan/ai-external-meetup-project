# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.4

# Agent URLs — comma-separated list of sub-agent base URLs for A2A discovery
AGENT_URLS=http://localhost:5001,http://localhost:5002

# MCP URL — where the recruiting agent connects to the MCP server
MCP_URL=http://localhost:5003
